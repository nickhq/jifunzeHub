<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.nkossy.jifunzeHub">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FlashCards.FlashCardsActivity" />
        <activity android:name=".FlashCards.BiologyFlashCardActivity" />
        <activity android:name=".UserActivity">

        </activity>
        <activity
            android:name=".Quiz.QuizActivity"
            android:theme="@style/AppTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Quiz.QuestionActivity" />
        <activity android:name=".Quiz.ResultActivity" />
        <activity android:name=".BaseActivity"/>
    </application>

</manifest>